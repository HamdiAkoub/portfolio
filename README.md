Pull image, create your own .env file and include inside it:
SECRET_KEY
ALLOWED_HOSTS
a default .env.example is implemented and will be used as default values if you do not wish to create your own .env.
